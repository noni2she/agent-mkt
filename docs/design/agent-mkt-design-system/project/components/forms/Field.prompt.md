Labeled single-line input with optional hint and invalid state.

```jsx
<Field label="關鍵字" mono value={kw} onChange={e => setKw(e.target.value)} placeholder="房地產" />
<Field label="最低讚數" type="number" hint="popular 門檻" value="500" />
```
