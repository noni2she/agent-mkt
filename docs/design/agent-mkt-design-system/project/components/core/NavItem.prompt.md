Sidebar navigation row. Shows icon + label, an active (brand-soft) state, and an optional count pill.

```jsx
<NavItem icon={<i data-lucide="radar" />} label="海巡" count={3} active />
<NavItem icon={<i data-lucide="inbox" />} label="審核佇列" count={12} />
<NavItem icon={<i data-lucide="book-marked" />} label="知識庫" />
```
