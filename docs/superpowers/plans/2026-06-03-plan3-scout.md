# Plan 3: 海巡(B) — content script 抓取 + 本地 filter + scout 指令 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development。
>
> **執行紀律（使用者偏好）**：速度優先、**不 over-design、只做必要功能**、**不寫前期 unit test**（驗證靠手動 e2e + build/typecheck），測試後補。

**Goal:** 後端下 `scout` 指令 → SW 轉給 Threads 分頁的 content script → content script 捲動抓取真實貼文、用 `core/filter` 本地篩、湊一批候選 → 回傳後端記錄。完成「海巡(B)」端到端（不含 LLM/儲存，那是 Plan 4）。

**Architecture:** 沿用 Plan 2 polling 通道。新增：① `scout` 指令（core/protocol）② content script（注入 threads.com，抓 DOM + 本地 `core/filter` + budget 迴圈）③ SW↔content script 用 `chrome.tabs.sendMessage` 轉發。**首次把 `core` 匯入擴充建置**（filter/types 必須前端跑）。content script 只讀 DOM + 與 SW 訊息往來，不直接 fetch 後端（避開頁面 CSP）。

**Tech Stack:** TypeScript、WXT、`core/filter`+`core/types`、既有 PoC 選擇器（`src/skills/scout/playwright.ts`）。

參考 spec §4/§5/§8。**選擇器可能因 Threads 改版需實測微調**——content script 會 log 抓到的數量以利除錯。

---

### Task 1: protocol 加 scout 指令 + 候選貼文 payload 形狀

**Files:** Modify `src/core/protocol.ts`

- [ ] **Step 1: 擴充 CommandSchema 與新增 ScoutCandidate**

在 `src/core/protocol.ts`，把 `CommandSchema` 改成含 scout，並加候選貼文型別：
```ts
/** 海巡 budget（先到先停）。 */
export const ScoutBudgetSchema = z.object({
  targetCandidates: z.number().default(10),
  maxScrolls: z.number().default(30),
  maxScanned: z.number().default(60),
});
export type ScoutBudget = z.infer<typeof ScoutBudgetSchema>;

/** 後端 → hands 的指令。 */
export const CommandSchema = z.discriminatedUnion("action", [
  z.object({ action: z.literal("ping") }),
  z.object({
    action: z.literal("scout"),
    keyword: z.string(),
    budget: ScoutBudgetSchema.partial().optional(),
  }),
]);
export type Command = z.infer<typeof CommandSchema>;

/** content script 抓到的候選貼文（給後端後續 LLM 判斷；MVP 部分欄位 best-effort）。 */
export const ScoutCandidateSchema = z.object({
  id: z.string(),
  url: z.string(),
  author_handle: z.string(),
  text: z.string(),
  likes: z.number(),
  replies: z.number(),
  popular_reason: z.string(),
});
export type ScoutCandidate = z.infer<typeof ScoutCandidateSchema>;
```
（其餘 Hello/Request/Response/parseClientMessage 不動。`ResponseEnvelope.payload` 仍是 `z.unknown()`，scout 結果放 payload，不需改 schema。）

- [ ] **Step 2: typecheck + 既有測試**

Run: `npm run typecheck && npm test`
Expected: 乾淨；既有 protocol 測試仍綠（ping 仍合法；新增 scout 不破壞）。

- [ ] **Step 3: Commit**
```bash
git add src/core/protocol.ts
git commit -m "feat(core): add scout command + ScoutCandidate to protocol"
```

---

### Task 2: content script — 抓取 + 本地 filter（注入 threads.com）

**Files:**
- Create `entrypoints/content.ts`
- Modify `wxt.config.ts`（host_permissions 加 threads.com）

- [ ] **Step 1: wxt.config.ts 加 Threads 權限**

把 `host_permissions` 改為：
```ts
    host_permissions: [
      "http://localhost/*",
      "http://127.0.0.1/*",
      "https://www.threads.com/*",
      "https://www.threads.net/*",
    ],
```

- [ ] **Step 2: 建 `entrypoints/content.ts`**

```ts
import type { ScoutCandidate, ScoutBudget } from "../src/core/protocol";
import { parseCount } from "../src/core/filter";

// content script 注入 Threads；收 SW 的 scout 訊息 → 捲動抓取 → 回傳候選。
// 只讀 DOM + 與 SW 訊息往來，不直接連後端（避開頁面 CSP）。
export default defineContentScript({
  matches: ["https://www.threads.com/*", "https://www.threads.net/*"],
  async main() {
    chrome.runtime.onMessage.addListener((msg, _sender, sendResponse) => {
      if (msg?.type === "scout") {
        scout(msg.keyword as string, msg.budget as Partial<ScoutBudget> | undefined)
          .then((candidates) => sendResponse({ ok: true, candidates }))
          .catch((e) => sendResponse({ ok: false, error: String(e) }));
        return true; // async sendResponse
      }
      return false;
    });
    console.log("[scout] content script ready");
  },
});

const SEL = {
  post: 'div[data-pressable-container="true"]',
  postText: '[data-testid="post-text"], div[dir="auto"]',
  likeBtn: 'svg[aria-label*="讚"], svg[aria-label*="ike"]',
};

const sleep = (ms: number) => new Promise((r) => setTimeout(r, ms));
const jitter = (mean: number) => Math.max(300, Math.round(mean * (0.6 + Math.random() * 0.8)));

async function scout(keyword: string, b?: Partial<ScoutBudget>): Promise<ScoutCandidate[]> {
  const targetCandidates = b?.targetCandidates ?? 10;
  const maxScrolls = b?.maxScrolls ?? 30;
  const maxScanned = b?.maxScanned ?? 60;

  if (!location.href.includes("/search")) {
    location.href = `https://www.threads.com/search?q=${encodeURIComponent(keyword)}&serp_type=default`;
    await sleep(3000); // 導頁後 DOM 重載；content script 會在新頁重新注入，本次先回空
    return [];
  }

  const out: ScoutCandidate[] = [];
  const seen = new Set<string>();
  let scrolls = 0;
  let scanned = 0;

  while (out.length < targetCandidates && scrolls < maxScrolls && scanned < maxScanned) {
    const cards = Array.from(document.querySelectorAll<HTMLElement>(SEL.post));
    for (const card of cards) {
      if (out.length >= targetCandidates || scanned >= maxScanned) break;
      const link = card.querySelector<HTMLAnchorElement>('a[href*="/post/"]');
      const href = link?.getAttribute("href") ?? "";
      const id = href.split("/post/")[1]?.split(/[?/]/)[0] ?? "";
      if (!id || seen.has(id)) continue;
      seen.add(id);
      scanned += 1;

      const text = (card.querySelector(SEL.postText)?.textContent ?? "").trim();
      if (!text) continue;
      const aria = card.querySelector(SEL.likeBtn)?.getAttribute("aria-label") ?? "";
      const likes = parseCount(aria);
      const author = (card.querySelector('a[href^="/@"]')?.textContent ?? "unknown").replace(/^@/, "").trim();

      // MVP 本地篩：可靠的「讚數門檻」為主（replies/時間 DOM 不穩，留後端/後續處理）
      if (likes < 100) continue;

      out.push({
        id,
        url: href.startsWith("http") ? href : `https://www.threads.com${href}`,
        author_handle: author,
        text,
        likes,
        replies: 0, // best-effort：DOM 不穩定，先 0，後端可後處理
        popular_reason: `👍${likes}`,
      });
    }
    window.scrollBy(0, 700 + Math.floor(Math.random() * 400));
    await sleep(jitter(1500));
    scrolls += 1;
  }
  console.log(`[scout] keyword="${keyword}" scanned=${scanned} scrolls=${scrolls} candidates=${out.length}`);
  return out;
}
```

> 註：此處先用「likes ≥ 100」硬門檻當本地篩的 MVP；之後 Plan 4 會把 `core/filter` 的完整 `isPopular`（含 rules）接進來（届時 rules 從後端傳入或由後端篩）。本任務先證明「抓取 + 回傳」鏈路通。

- [ ] **Step 3: 建置 + typecheck**

Run: `npm run ext:build && npm run typecheck`
Expected: 建置成功（content script 出現在 `dist/chrome-mv3/`，且 `core/filter`、`core/protocol` 被 bundle 進去）；後端 typecheck 不受影響。
- 若 WXT 對跨 `../src/core` 匯入報路徑/解析錯誤，於 `wxt.config.ts` 確認 vite 能解析（必要時加 `alias`），並報告所需調整。

- [ ] **Step 4: Commit**
```bash
git add entrypoints/content.ts wxt.config.ts
git commit -m "feat(ext): content script scout (scrape + local likes filter) on Threads"
```

---

### Task 3: SW 轉發 scout 指令到 content script

**Files:** Modify `entrypoints/background.ts`

- [ ] **Step 1: 在 background 的指令處理加 scout 分支**

把 `pollOnce` 內的指令迴圈改為支援 scout（保留 ping）：
```ts
    for (const { id, command } of cmds) {
      if (command.action === "ping") {
        console.log("[hands] ping → pong", id);
        await postResult({ type: "response", id, status: "ok", payload: "pong" });
      } else if (command.action === "scout") {
        await handleScout(id, command);
      }
    }
```
並新增 handleScout（找 Threads 分頁、轉發訊息、回傳結果）：
```ts
  async function handleScout(id: string, command: { keyword: string; budget?: unknown }) {
    const tabs = await chrome.tabs.query({ url: ["https://www.threads.com/*", "https://www.threads.net/*"] });
    const tab = tabs[0];
    if (!tab?.id) {
      console.log("[hands] scout: 沒有開著的 Threads 分頁");
      await postResult({ type: "response", id, status: "element_not_found", error: "no threads tab" });
      return;
    }
    try {
      const res = await chrome.tabs.sendMessage(tab.id, {
        type: "scout",
        keyword: command.keyword,
        budget: command.budget,
      });
      if (res?.ok) {
        console.log(`[hands] scout 完成，候選 ${res.candidates.length} 篇`);
        await postResult({ type: "response", id, status: "ok", payload: res.candidates });
      } else {
        await postResult({ type: "response", id, status: "fail", error: res?.error ?? "scout failed" });
      }
    } catch (e) {
      await postResult({ type: "response", id, status: "fail", error: String(e) });
    }
  }
```
其餘（alarms/poll/postResult/ping）不動。

- [ ] **Step 2: 加 tabs 權限**

`wxt.config.ts` 的 `permissions` 改為 `["alarms", "tabs"]`（query/sendMessage 需要）。

- [ ] **Step 3: 建置 + typecheck**

Run: `npm run ext:build && npm run typecheck`
Expected: 成功。

- [ ] **Step 4: Commit**
```bash
git add entrypoints/background.ts wxt.config.ts
git commit -m "feat(ext): SW relays scout command to Threads content script"
```

---

### Task 4: 後端 dev 觸發 scout + 記錄結果

**Files:** Modify `src/backend/main.ts`

- [ ] **Step 1: 加 env-gated scout 觸發**

在 `server.listen` callback 內、ping 區塊之後加：
```ts
  if (process.env.DEV_SCOUT) {
    const keyword = process.env.DEV_SCOUT;
    console.log(`[dev] DEV_SCOUT：10s 後對 tenant 'us' 下 scout("${keyword}")`);
    setTimeout(async () => {
      try {
        const r = await queue.enqueue("us", { action: "scout", keyword }, 60_000);
        const posts = Array.isArray(r.payload) ? r.payload : [];
        console.log(`[dev] scout 回傳 ${posts.length} 篇候選：`);
        for (const p of posts as Array<{ author_handle: string; likes: number; text: string }>) {
          console.log(`  - @${p.author_handle} 👍${p.likes} ${p.text.slice(0, 40).replace(/\n/g, " ")}…`);
        }
      } catch (e) {
        console.log("[dev] scout:", (e as Error).message);
      }
    }, 10_000);
  }
```

- [ ] **Step 2: typecheck + 測試**

Run: `npm run typecheck && npm test`
Expected: 乾淨、測試綠。

- [ ] **Step 3: Commit**
```bash
git add src/backend/main.ts
git commit -m "feat(backend): env-gated DEV_SCOUT trigger to verify scout round-trip"
```

---

### Task 5: 端到端手動驗證（真實 Threads）

**Files:** 無

- [ ] **Step 1:** 重新載入擴充（`chrome://extensions` → 該擴充 → 重新載入）以套用新建置。
- [ ] **Step 2:** 在 Chrome **登入 Threads** 並開著一個 `https://www.threads.com/...` 分頁。
- [ ] **Step 3:** 啟動後端並觸發 scout：
```bash
DEV_SCOUT=居家 npm run backend
```
（關鍵字可換。）
- [ ] **Step 4: 觀察（成功判準）**
  - content script console（在 Threads 分頁的 DevTools）：`[scout] content script ready` → `[scout] keyword=... scanned=N scrolls=M candidates=K`
  - 後端 console：`[dev] scout 回傳 K 篇候選：` 後列出幾筆 `@作者 👍讚數 文字…`
  - 成功＝後端印出非空候選清單（或至少 content script 有掃描動作、candidates 數合理）。
- [ ] **Step 5: 選擇器微調（若需要）**
  - 若 candidates=0 但頁面明明有貼文 → 選擇器可能因 Threads 改版失效。在 content script console 手動試 `document.querySelectorAll('div[data-pressable-container="true"]').length` 等，回報實際可用的 selector，據以調整 `SEL`，重建重載再試。
- [ ] **Step 6:** 記錄結果（兩側 log 摘錄）於報告。

---

## Self-Review

**Spec coverage：** §8 海巡（本地、先到先停 budget、捲動抓取、本地篩、取樣非排名）→ Task 2 ✅；§4 core 共用（filter/types 匯入擴充）→ Task 2 ✅；§5 hands 指令詞彙 scout → Task 1/3 ✅；通道沿用 Plan 2 polling ✅。

**YAGNI：** 本地篩先用 likes 硬門檻（完整 rules/isPopular 留 Plan 4 接後端）；replies/時間 best-effort；無 LLM、無儲存（Plan 4）；無 content script↔SW 以外的多餘抽象；無前期 unit test。

**一致性：** `scout` 指令符合擴充後的 `CommandSchema`；候選符合 `ScoutCandidateSchema`；結果走 `ResponseEnvelope.payload`（已是 unknown）；SW 用 `chrome.tabs.sendMessage` 轉發、content script 用 `onMessage`+async sendResponse。

**已知風險：** Threads 選擇器可能過時（Task 5 Step 5 有實測微調流程）；replies/posted_at 不準（MVP 接受）。

---

## 後續（不在本檔）

- **Plan 4**：後端收 scout 候選 → `core/filter` 完整 isPopular（rules）→ LLM 寫 reply 草稿 → 存 SQLite review_item。
- 之後：審核台側欄、發布(B)。
